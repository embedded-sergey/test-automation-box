############################################
# TEST CASE 1: clean input for import meas #
############################################
#
# DESCRIPTION: 
# The import.meas() function should be able to work with an input file of 
# the FishResp format. To test it, the "test_data_overnight" file is used.
#
# ENVIRONMENT SETUP:
# 1. R, version > 4.0.0
# 2. R package 'FishResp', version under test
# 3. The "test_data_overnight_NA" file" that contains inserted NA, i.e.:


################################################
# TEST CASE 2: Input with NA for import.meas() #
################################################
#
# DESCRIPTION: 
# The import.meas() function should be able to work with any NA data in 
# the input data. To test it, the "test_data_multinight_NA" file is used 
# containing data with inserted NA values in various locations.
# 
# ENVIRONMENT SETUP:
# 1. R, version > 4.0.0
# 2. R package 'FishResp', version under test
# 3. The "test_data_multinight_NA" file " that contains inserted NA, i.e.:
#    Ox.1 in M1 - all data NA
#    Temp.2 in M2 - all data NA
#    Ox.3 in M3 - some NA in beginning, middle, and end
#    Temp.4 in M4 - some NA in beginning, middle, and end
#    Temp.1, Ox.1, Temp.2, Ox.2, Temp.3, Ox.3, Temp.4, Ox.4 in M5 - some NA
#    Temp.1, Ox.1, Temp.2, Ox.2, Temp.3, Ox.3, Temp.4, Ox.4 in M6 - all NA
#
# TEST EXECUTION:
# 1. Open R 
# 2. Load the library under test "FishResp"
# 3. Run the function import.info() with default values
# 4. Run the function import.meas() with the file "test_data_multinight_NA" 
#
# EXIT CRITERIA:
# If an output file of import.meas() should exist and be > 100 bytes: PASSED
# If the output file is empty, an error message appears, or out of time: FAILED


####################
### Test suite 3 ###
####################
#
# The file "test_data_multinight_NA_missed" contains data with inserted NA. 
#
3. "test_data_multinight_NA_miss" is data with inserted NA + missed lines/phases:
	- // -
	All columns in M7 - some lines are missing in beginning, middle and end
	All columns in M8 - all lines are missing
	All columns in M9 - one line are presented
	All columns in M10 - two lines are presented
	All columns in M100 - all lines are missing overnight
	All columns in M200100 - all lines for one minute after midnight is missing

4. "test_data_multinight_NA_miss_lag":
	- // -
	+
	+ 

4. "test_data_overnight" is data overnight from 10 AM to 7 AM
	the parameters 
	
	